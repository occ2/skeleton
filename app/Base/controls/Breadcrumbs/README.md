annotations config from presenter
@breadcrumbs
    key - (string) unique key of breadcrumb
    name - (string) text in anchor
    link - (string) presenter link
    active - (bool) is link active or not? 
